# Shell scripts must use LF so they run inside Linux containers.
# Without this, Windows checkouts get CRLF and the shebang breaks:
#   exec /app/entrypoint.sh: no such file or directory
*.sh text eol=lf
entrypoint.sh text eol=lf
