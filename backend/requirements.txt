Django>=5.0,<6.0
djangorestframework>=3.14.0
psycopg2-binary>=2.9.9
celery>=5.3.0
redis>=5.0.0
django-elasticsearch-dsl>=8.0.0
elasticsearch-dsl>=8.0.0
django-storages>=1.14.0
boto3>=1.29.0
gunicorn>=21.2.0
tika>=2.6.0
langdetect>=1.0.9
django-cors-headers>=4.3.0
djangorestframework-simplejwt>=5.3.0
dj-rest-auth>=5.0.0
django-allauth>=0.61.1
cryptography>=42.0.0
requests>=2.31.0
Pillow>=10.0.0
pdf2image>=1.17.0
google-generativeai>=0.5.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-anthropic>=0.3.0
langchain-openai>=0.2.0

# --- CopilotKit deep-agent sidecar (agent_service/, runs on FastAPI/uvicorn) ---
# Installed into the shared backend image so the `agent` compose service can run
# `uvicorn agent_service.main:app`. Requires Python >=3.11 (base image is 3.11).
deepagents>=0.6.0
langgraph>=0.2.0
copilotkit>=0.1.9
ag-ui-langgraph>=0.0.1
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0

# KB extraction pipeline (extraction/kb/)
pydantic>=2.7,<3          # heritage schema (was only a transitive dep before)
openai>=1.40.0            # Stage 1 split via OpenRouter (was transitive via langchain-openai)
google-genai>=1.20.0      # Stage 2 Gemini structured output (new SDK; the legacy
                          # google-generativeai above stays for semantic embeddings)
